<div id="<%= dom_id transaction %>" class="card mb-3">
  <div class="card-body d-flex justify-content-between align-items-center">
    <div>
      <h5 class="card-title"><%= group_deal.deal.name %></h5>
      <p><%= transaction.created_at.strftime("%d %b, %I:%M %p") %></p>
    </div>
    <div>
      <p class="mb-0">₵<%= group_deal.deal.amount %></p>
      <p><%= transaction.group.name %></p>
    </div>
  </div>
</div>
