<%= render "shared/navbar", title: @group.name + " Transactions" %>
<div id="group_deals" style="margin-top: 5rem;">
  <div class="my-4 d-flex justify-content-between align-items-center">
    <%= link_to "Back to Categories", user_groups_path(current_user) %>
    <p>Total Amount: ₵<%= @group.total_amount_of_transactions %></p>
  </div>
  <% @group.recent_transactions.each do |transaction| %>
    <%= render transaction %>
  <% end %>
  <% if @group.group_expenses.empty? %>
    <div class="alert alert-danger" role="alert">
      <h4 class="alert-heading text-center">There are no transactions</h4>
    </div>
  <% end %>
</div>
<div class="add-cat">
  <%= link_to new_user_group_expense_path(current_user, @group), class: "btn btn-info" do %>
    <svg xmlns="http://www.w3.org/2000/svg" width="45" height="45" fill="#fff" class="bi bi-plus-lg" viewBox="0 0 16 16">
      <path fill-rule="evenodd" d="M8 2a.5.5 0 0 1 .5.5v5h5a.5.5 0 0 1 0 1h-5v5a.5.5 0 0 1-1 0v-5h-5a.5.5 0 0 1 0-1h5v-5A.5.5 0 0 1 8 2Z"/>
    </svg>
  <% end %>
</div>
